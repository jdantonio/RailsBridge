<% if @errors %>
  <div id="error_explanation">
    <h2><%= pluralize(@errors.count, 'error') %> prohibited this topic from being saved:</h2>

    <ul>
    <% @errors.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= label(:topic, :title) %><br>
  <%= text_field(:topic, :title) %>
</div>
<div class="field">
  <%= label(:topic, :description) %><br>
  <%= text_field(:topic, :description) %>
</div>
